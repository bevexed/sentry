<clickhouse>
  <profiles>
    <default>
      <!-- 单个查询最大内存 4GB -->
      <max_memory_usage>4000000000</max_memory_usage>
      <!-- 所有查询总内存不超过 5GB -->
      <max_memory_usage_for_all_queries>5000000000</max_memory_usage_for_all_queries>
      <!-- 外部聚合阈值 800MB -->
      <max_bytes_before_external_group_by>800000000</max_bytes_before_external_group_by>
      <!-- 外部排序阈值 800MB -->
      <max_bytes_before_external_sort>800000000</max_bytes_before_external_sort>
    </default>
  </profiles>

  <!-- 服务端全局内存限制 -->
  <max_server_memory_usage_to_ram_ratio>0.8</max_server_memory_usage_to_ram_ratio>

  <!-- 后台合并使用的内存限制 -->
  <merge_tree>
    <max_bytes_to_merge_at_max_space_in_pool>536870912</max_bytes_to_merge_at_max_space_in_pool>
    <number_of_free_entries_in_pool_to_execute_mutation>3</number_of_free_entries_in_pool_to_execute_mutation>
  </merge_tree>

  <!-- 禁用 trace_log（生产环境用不到，但默认会占大量磁盘） -->
  <trace_log remove="1"/>

  <!-- 其余 system 日志限制 14 天 TTL，避免磁盘被撑满 -->
  <query_log>
    <engine>ENGINE = MergeTree PARTITION BY event_date ORDER BY (event_time) TTL event_date + INTERVAL 14 DAY DELETE</engine>
    <partition_by remove="1"/>
    <ttl remove="1"/>
  </query_log>
  <metric_log>
    <engine>ENGINE = MergeTree PARTITION BY event_date ORDER BY (event_time) TTL event_date + INTERVAL 14 DAY DELETE</engine>
    <partition_by remove="1"/>
    <ttl remove="1"/>
  </metric_log>
  <text_log>
    <engine>ENGINE = MergeTree PARTITION BY event_date ORDER BY (event_time) TTL event_date + INTERVAL 14 DAY DELETE</engine>
    <partition_by remove="1"/>
    <ttl remove="1"/>
  </text_log>
  <asynchronous_metric_log>
    <engine>ENGINE = MergeTree PARTITION BY event_date ORDER BY (event_time) TTL event_date + INTERVAL 14 DAY DELETE</engine>
    <partition_by remove="1"/>
    <ttl remove="1"/>
  </asynchronous_metric_log>
  <part_log>
    <engine>ENGINE = MergeTree PARTITION BY event_date ORDER BY (event_time) TTL event_date + INTERVAL 14 DAY DELETE</engine>
    <partition_by remove="1"/>
    <ttl remove="1"/>
  </part_log>
  <processors_profile_log>
    <engine>ENGINE = MergeTree PARTITION BY event_date ORDER BY (event_time) TTL event_date + INTERVAL 14 DAY DELETE</engine>
    <partition_by remove="1"/>
    <ttl remove="1"/>
  </processors_profile_log>
</clickhouse>
